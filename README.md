# 📝 To-Do List App (Vanilla JS + Vite)

A simple To-Do List app built with **HTML, CSS, and JavaScript** using **Vite** as the dev server.

## Features
- Add new tasks
- Mark tasks as completed
- Delete tasks
- Persistent storage via `localStorage`
- Hot reload with Vite

## Run Locally
```bash
npm install
npm run dev
# ToDoTest
